# ================= Rihem HR System — Python dependencies =================
# Core API
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# Database
supabase>=2.4.0

# Attendance / face (existing app.py)
opencv-python>=4.9.0
numpy>=1.26.0
# face_recognition is optional/heavy — install separately if doing real face matching:
# face_recognition>=1.3.0

# ================= AI / LLM recruitment module =================
groq>=0.11.0          # Groq LLM inference (Llama 3.3, etc.)
pdfplumber>=0.11.0    # CV PDF text extraction
python-docx>=1.1.0    # CV .docx text extraction (optional)
